SeguimientoInstrucciones:
    def __init__(self):
        self.sumclass a = 0.0
        self.x = 0.0
        self.y = 0.0

    def ejecutar_seguimiento(self) -> float:
        self.suma = 0
        self.x = 20
        self.suma = self.suma + self.x
        self.y = 40
        self.x = self.x + (self.y ** 2)
        self.suma = self.suma + (self.x / self.y)
        return self.suma


def main():
    seguimiento = SeguimientoInstrucciones()
    resultado = seguimiento.ejecutar_seguimiento()
    print(f"EL VALOR DE LA SUMA ES: {resultado}")


if __name__ == "__main__":
    main()
